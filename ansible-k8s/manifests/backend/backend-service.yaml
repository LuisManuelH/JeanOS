apiVersion: v1
kind: Service
metadata:
  name: jeanos-backend-service
  namespace: jeanos-shop
spec:
  type: ClusterIP
  selector:
    app: jeanos-backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
