apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-config
  namespace: jeanos-shop
data:
  redis.conf: |
    bind 0.0.0.0
    port 6379
    protected-mode no

    maxmemory 256mb
    maxmemory-policy allkeys-lru

    appendonly no
    save ""
