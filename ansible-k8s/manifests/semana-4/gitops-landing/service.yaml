apiVersion: v1
kind: Service
metadata:
  name: gitops-landing
  namespace: semana4-gitops
  labels:
    app: gitops-landing
    app.kubernetes.io/part-of: semana4-gitops
spec:
  type: NodePort
  selector:
    app: gitops-landing
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31080
